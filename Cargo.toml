[workspace]
resolver = "3"
members = [
    "utils",
    "macros",
    "drivers",
    "scheduler", 
    "hypervisor",
    "boot",
    "logger", 
    "pmm", 
    "slab", 
    "arch",
]

[profile.dev]
debug = true
panic = "abort"

[profile.release]
panic = "abort"

