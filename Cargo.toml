[package]
name = "funderberker"
version = "0.1.0"
edition = "2024"

[dependencies]
limine = { version = "0.3.1", optional = true }

[features]
default = ["limine", "serial"]
# boot options
limine = ["dep:limine"]
# logging options
serial = []
framebuffer = []
