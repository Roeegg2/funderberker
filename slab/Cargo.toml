[package]
name = "slab"
version = "0.1.0"
edition = "2024"

[dependencies]
utils = { version = "0.1.0", path = "../utils" }
kernel = { version = "0.1.0", path = "../kernel" }
limine = { version = "0.5.0", optional = true }

[lints.clippy]
pedantic = "warn"
